// Copy this file to src/config.js and fill in your Supabase details for local development.
// These values are injected into the browser via the global window object so the app
// can stay 100% static (just open index.html).
window.SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
// You can also set SUPABASE_URL and SUPABASE_ANON_KEY as environment variables (see .env.example)
// when running the Vite dev server instead of using global window variables.
