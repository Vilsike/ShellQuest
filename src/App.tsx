import React from "react";

export default function App() {
  return (
    <div style={{ minHeight: "100vh", padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>ShellQuest</h1>
      <p>React app is running. Next: wire the ShellQuest UI module back in.</p>
    </div>
  );
}
